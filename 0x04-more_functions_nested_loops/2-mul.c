#include "main.h"

/**
 * mul - Multiplies two integers
 * @x: The first number to multiply
 * @y: The second number to mutiply with the first number
 * Return: The the result of multiplication between two parameters
 */

int mul(int x, int y)
{
	return (x * y);
}
